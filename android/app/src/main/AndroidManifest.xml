<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SandboxApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            CDN base URL for miniapp page bundles.
            MainActivity.loadPageBundle 가 이걸 읽어서 https://{base}/android/pages/{appName}.bundle.js 를
            OkHttp 로 fetch → cacheDir 에 임시 저장 → JSBundleLoader.createFileLoader 로 평가.
            life monorepo 의 `yarn deploy:cdn:android` 가 동일 prefix(android/pages/)로 업로드.
        -->
        <meta-data
            android:name="LifePlusCDNBaseURL"
            android:value="https://d23eo46tuoxssl.cloudfront.net" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lifeplus-tribes" />
            </intent-filter>
        </activity>
    </application>
</manifest>
